export class Tree {
  insert(value) {}

  // Breadth-first search
  BFS(visitor) {}

  // Pre-order depth-first search
  preOrderDFS(visitor) {}

  // Post-order depth-first search
  postOrderDFS(visitor) {}
}
